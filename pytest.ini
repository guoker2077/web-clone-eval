[pytest]
# 一键单测：仅跑离线、确定、秒级的核心逻辑测试（不联网、不调 Claude API）。
# 跑法：docker compose run --rm pipeline pytest        （或本地 pytest）
testpaths = tests/unit
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short
